#ifndef PASSGEN
#define PASSGEN

#include <string>

std::string genSet();
char genChar(char);
std::string genCode ();
char useNum();
char useCase();
char useChar();
int len();
std::string lowercase(std::string);
std::string encrypt(std::string);
std::string decrypt(std::string);

#endif
